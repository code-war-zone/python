# **Installing Python and IDE Setup**

This guide will help you install Python on your system and set up a development environment using Visual Studio Code (VS Code).
By the end, you’ll be ready to write and run your first Python programs.

---

## 🐍 **1. Downloading & Installing Python**

### ✔ Step 1 — Download Python

Go to the official Python website:

👉 **[https://www.python.org/downloads/](https://www.python.org/downloads/)**

Choose the latest stable version (Python 3.x).

---

### ✔ Step 2 — Install Python

During installation:

#### **Windows Users**

Make sure to check:

```
☑ Add Python to PATH
```

Then click **Install Now**.

#### **macOS Users**

Download the `.pkg` installer and run it normally.

#### **Linux Users**

Most distributions include Python.
To install or upgrade:

```bash
sudo apt install python3
```

---

### ✔ Step 3 — Verify Installation

Open a terminal (Command Prompt, PowerShell, macOS Terminal) and type:

```bash
python --version
```

or:

```bash
python3 --version
```

You should see something like:

```
Python 3.x.x
```

---

## 🖥 **2. Installing Visual Studio Code (VS Code)**

VS Code is a lightweight, modern editor perfect for Python development.

Download it here:

👉 **[https://code.visualstudio.com/](https://code.visualstudio.com/)**

Install it like any other application.

---

## 🔌 **3. Install the Python Extension in VS Code**

Open VS Code → Go to **Extensions** (left sidebar) → Search for:

```
Python (Microsoft)
```

Install it.
This extension provides:

* Code highlighting
* Auto-complete (IntelliSense)
* Code formatting
* Debugging support
* Virtual environment integration

---

## 🧪 **4. Creating Your First Python File**

1. Open a folder in VS Code
2. Create a new file named:

```
hello.py
```

3. Write:

```python
print("Hello, Python!")
```

---

## ▶️ **5. Running Python Code**

You can run your script in two ways:

### **A. Using the Terminal**

Open the VS Code terminal:

```
View → Terminal
```

Then run:

```bash
python hello.py
```

or (some systems):

```bash
python3 hello.py
```

---

### **B. Using the Run Button**

VS Code may show a **Run Python File** button at the top right.

Click it to execute your script.

---

## 🎉 **Setup Complete**

You now have:

✔ Python installed
✔ VS Code ready
✔ The Python extension installed
✔ The ability to run Python scripts

You're ready to start coding!

---

